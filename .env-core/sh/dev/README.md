# Local scratch (`sh/dev`)

This folder is **not** sourced by `autoloader.sh`. Keep experiments, drafts, and one-off scripts here.

- `draft/` — old prototypes (safe to delete locally)
- `plugins/` — optional bash helpers for manual `source`
- `*.sh` at this level — manual utilities only

Tracked in git: this README only (see root `.gitignore`).
